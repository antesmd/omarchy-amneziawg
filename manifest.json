{
  "schemaVersion": 1,
  "id": "antesmd.amneziawg",
  "name": "Omazia",
  "version": "1.2.0",
  "author": "antesmd",
  "license": "MIT",
  "description": "Tunnel status, per-tunnel toggles for running several at once, import, rename, QR export, live traffic and drop notifications in the Omarchy bar. Manages AmneziaWG tunnels under /etc/amnezia/amneziawg via awg-quick and a small root helper invoked with pkexec — no NetworkManager, no sudo shell. Unofficial third-party widget; not affiliated with the AmneziaWG project.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Omazia",
    "description": "Toggle AmneziaWG tunnels one by one (run as many as you like at once), rename them, import .conf files, show QR codes, watch traffic, get a toast when a tunnel drops. Drives awg-quick through a pkexec helper — no NetworkManager, one-time sudo ./install.sh for the polkit policy.",
    "category": "Network",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshIntervalSec": 10,
      "pingHost": "1.1.1.1"
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval (seconds)",
        "min": 2,
        "max": 3600,
        "step": 1,
        "defaultValue": 10
      },
      {
        "key": "pingHost",
        "type": "string",
        "label": "Ping host (empty disables the latency probe)",
        "defaultValue": "1.1.1.1"
      }
    ]
  }
}
